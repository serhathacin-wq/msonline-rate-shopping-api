{
    "name": "msonline-rate-shopping-api",
    "version": "1.0.0",
    "description": "Vercel serverless API — anlık otel fiyat karşılaştırma (12 OTA).",
    "private": true,
    "type": "module",
    "engines": {
        "node": ">=18.0.0"
    },
    "scripts": {
        "dev": "vercel dev",
        "deploy": "vercel --prod"
    }
}
